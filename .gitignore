tmp/*
config/database.php
app/tmp/*
app/config/database.php
!empty
# UNIX editor backup files
*~
# Packages (inefficient, shouldn't be source controlled)
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip
# Temp files created by the Vim editor
*.swp
# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Icon?
ehthumbs.db
Thumbs.db
